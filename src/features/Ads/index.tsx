import React from "react";
import styles from "./styles.module.scss";

const Advertisement: React.FC = () => {
  return (
    <div className={styles.advertisement}>Your advertisement could be here</div>
  );
};

export default Advertisement;
